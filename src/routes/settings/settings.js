import style from './settings.scss'

const Settings = () => {
    return (
        <section className={style.settings}>
            <h1>Settings</h1>
            <p>TODO</p>
        </section>
    )
}

export default Settings
